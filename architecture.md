/nouns-builder-launch-cli
├── src/
│   ├── modules/          # Core handoff modules
│   │   └── ExampleModule.ts
│   ├── types/            # Type definitions (Module interface)
│   ├── utils/            # Handoff helpers (Viem clients, etc.)
│   └── index.ts          # CLI entry point (Dry-run logic)
├── test/                 # Security test suites
├── .eslintrc.json        # Linting (Strict enforcement)
├── tsconfig.json         # Strict TypeScript config
├── package.json          # Dependencies & Scripts
└── README.md